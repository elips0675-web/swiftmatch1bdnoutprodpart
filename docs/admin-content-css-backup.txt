# CSS Backup — admin-content.tsx (актуально, 05.09.2026)
# Эталон текущего состояния кнопок/бейджей на вкладках Интересы / Цели / Образование / Города / Запрещённые слова.
# ЕДИНЫЙ стиль бейджа во ВСЕХ секциях (общий компонент EditableList).

## Badge item (EditableList, строка 68) — все секции
className="text-sm py-1 px-3 rounded-full border bg-background shadow-sm flex items-center gap-1.5 whitespace-nowrap leading-tight"

## Badge item in countries cities (строка ~258) — города внутри «Страны»
className="text-sm py-1 px-3 rounded-full border bg-background shadow-sm flex items-center gap-1.5 whitespace-nowrap leading-tight"

## Items container (строка 66) — НЕ содержит min-h, НЕ растягивает (items-start)
className="flex flex-wrap items-start gap-2 p-4 rounded-2xl border bg-muted/30"

## Save button
className="rounded-xl"

## Add button (строка 78)
className="rounded-xl h-10 px-6"

## CSV button (строка 61)
className="ml-auto h-7 text-xs"

## Delete icon in badge
className="text-muted-foreground hover:text-destructive transition-colors"

## Country add button
className="rounded-xl h-10 px-6"

## City add button
className="rounded-xl h-9"

## Collapsible trigger
className="flex w-full items-center justify-between p-3 rounded-xl border bg-muted/30 hover:bg-muted/50 transition-colors"

## Delete country
className="text-muted-foreground hover:text-destructive transition-colors p-1"

## Input (общий)
className="h-10 rounded-xl"

## Country input
className="h-10 rounded-xl"

## City input
className="h-9 rounded-xl text-sm"

## TabsList
className="grid w-full grid-cols-2 sm:grid-cols-5 h-auto p-1 bg-muted/50 rounded-xl mb-6"

## TabsTrigger
className="rounded-lg py-2 font-bold text-xs"

## Save footer wrapper
className="mt-2 flex justify-end"

# Логика (важно при откате):
# - onAdd ВО ВСЕХ секциях сразу вызывает handleSave (автосохранение добавления).
# - Секции сохраняются по ключам: interests, dating_goals, education, cities, banned_words.
# - В БД content_config секции должны лежать как JSON-массивы (не плоские строки).